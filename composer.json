{
    "repositories": [
        {
            "type":"composer",
            "url": "http://packages.phundament.com"
        }
    ],
    "require": {
        "php": ">=5.3.2",
        "yiisoft/yii": "1.1.16",
        "yiiext/migrate-command": "0.7.*"
    },
    "config": {
        "bin-dir": "bin/",
       "vendor-dir": "protected/vendor/"
    },
   "autoload":{
	      "psr-0":{
            "config":"www/protected/"
	      }
	}
}